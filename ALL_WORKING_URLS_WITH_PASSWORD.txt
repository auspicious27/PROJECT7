╔════════════════════════════════════════════════════════╗
║  MLOps Pipeline - ALL WORKING URLs + Jenkins Password ║
╚════════════════════════════════════════════════════════╝

✅ NEW INSTANCE CREATED WITH IMPROVED CONFIGURATION!

Instance ID: YOUR_INSTANCE_ID
Public IP: YOUR_INSTANCE_IP
Region: us-east-1
Instance Type: t3.micro

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 Web UI (Streamlit):
   http://YOUR_INSTANCE_IP:8501
   Interactive interface for making predictions

🔌 API (Flask):
   http://YOUR_INSTANCE_IP:5000
   REST API for programmatic access
   
   Test command:
   curl -X POST http://YOUR_INSTANCE_IP:5000/predict \
     -H "Content-Type: application/json" \
     -d '{"features": [5.1, 3.5, 1.4, 0.2]}'

📊 Prometheus (Monitoring):
   http://YOUR_INSTANCE_IP:9090
   Metrics and performance monitoring

🔄 Jenkins (CI/CD):
   http://YOUR_INSTANCE_IP:8080
   Continuous Integration and Deployment

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔑 JENKINS PASSWORD:

Password abhi generate ho raha hai. 5-10 minutes baad get karne ke liye:

Method 1: AWS Console se
1. AWS Console > EC2 > Instances
2. Instance select karein: YOUR_INSTANCE_ID
3. Connect > EC2 Instance Connect
4. Run command:
   sudo cat /var/lib/jenkins/secrets/initialAdminPassword

Method 2: SSH se (agar key pair hai)
   ssh ec2-user@YOUR_INSTANCE_IP
   sudo cat /var/lib/jenkins/secrets/initialAdminPassword

Method 3: Console Output se
   Password automatically /home/ec2-user/jenkins_info.txt mein save ho jayega

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT: Instance abhi setup ho raha hai!

Improved user data script automatically:
✅ Python 3.7 compatible requirements (Flask 2.3.3)
✅ Better error handling with retries
✅ Docker properly configured
✅ Services auto-start with proper logging
✅ Jenkins password saved to file

⏱️  Estimated time: 5-10 minutes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Quick Test Commands (After 5-10 minutes):

# Health check
curl http://YOUR_INSTANCE_IP:5000/health

# Make prediction
curl -X POST http://YOUR_INSTANCE_IP:5000/predict \
  -H "Content-Type: application/json" \
  -d '{"features": [5.1, 3.5, 1.4, 0.2]}'

# Check Prometheus
curl http://YOUR_INSTANCE_IP:9090/-/healthy

# Check Jenkins
curl http://YOUR_INSTANCE_IP:8080/login

