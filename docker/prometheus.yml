# Prometheus configuration for MLOps pipeline monitoring

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Flask API metrics
  - job_name: 'flask-api'
    static_configs:
      - targets: ['flask-api:5000']
    metrics_path: '/metrics'
    scrape_interval: 10s
