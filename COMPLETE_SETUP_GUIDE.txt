╔════════════════════════════════════════════════════════╗
║     COMPLETE SETUP GUIDE - ALL WORKING URLs            ║
╚════════════════════════════════════════════════════════╝

✅ INSTANCE CREATED SUCCESSFULLY!

Instance ID: YOUR_INSTANCE_ID
Public IP: YOUR_INSTANCE_IP
Region: us-east-1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 ALL SERVICE URLs:

1. 🌐 Web UI (Streamlit):
   http://YOUR_INSTANCE_IP:8501

2. 🔌 API (Flask):
   http://YOUR_INSTANCE_IP:5000

3. 📊 Prometheus (Monitoring):
   http://YOUR_INSTANCE_IP:9090

4. 🔄 Jenkins (CI/CD):
   http://YOUR_INSTANCE_IP:8080

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️  WAIT TIME: 5-10 minutes

Instance abhi automatically setup ho raha hai:
- Repository clone ho rahi hai
- Dependencies install ho rahi hain
- Models train ho rahe hain
- Docker services start ho rahi hain

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔑 JENKINS PASSWORD KAISE GET KAREIN:

Method 1: AWS Console (Easiest)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. AWS Console mein jao: https://console.aws.amazon.com/ec2
2. EC2 > Instances
3. Instance select karo: YOUR_INSTANCE_ID
4. "Connect" button click karo
5. "EC2 Instance Connect" tab select karo
6. "Connect" click karo
7. Terminal mein yeh command run karo:
   
   sudo cat /var/lib/jenkins/secrets/initialAdminPassword

8. Password copy karo aur Jenkins login mein use karo

Method 2: Console Output se
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Password automatically /home/ec2-user/jenkins_info.txt mein 
save ho jayega. AWS Console > EC2 > Instance > 
"Get system log" se bhi check kar sakte hain.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ SERVICES CHECK KARNE KE LIYE:

After 5-10 minutes, yeh commands run karo:

# API Health Check
curl http://YOUR_INSTANCE_IP:5000/health

# Make Prediction
curl -X POST http://YOUR_INSTANCE_IP:5000/predict \
  -H "Content-Type: application/json" \
  -d '{"features": [5.1, 3.5, 1.4, 0.2]}'

# Prometheus Check
curl http://YOUR_INSTANCE_IP:9090/-/healthy

# Jenkins Check
curl http://YOUR_INSTANCE_IP:8080/login

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 AGAR SERVICES START NAHI HUI TO:

1. AWS Console > EC2 > Instance > Connect > EC2 Instance Connect
2. Terminal mein yeh commands run karo:

   cd /home/ec2-user/devops-project-7
   sudo docker-compose ps
   sudo docker-compose logs
   sudo docker-compose up -d

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 JENKINS SETUP STEPS:

1. Browser mein open karo: http://YOUR_INSTANCE_IP:8080
2. Initial admin password enter karo (Method 1 se get karo)
3. "Install suggested plugins" click karo
4. Admin user create karo (ya skip karo)
5. Jenkins ready!

Pipeline setup ke liye:
   cd /home/ec2-user/devops-project-7
   sudo ./setup_jenkins_pipeline.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 QUICK SUMMARY:

✅ Instance Created: YOUR_INSTANCE_ID
✅ Public IP: YOUR_INSTANCE_IP
✅ All Ports Open: 5000, 8501, 9090, 8080
✅ Auto-Deployment: Running (5-10 min)
✅ Jenkins Password: AWS Console se get karo

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generated: $(date)

